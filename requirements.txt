# Core dependencies for mesh-to-3DGS conversion
numpy>=1.24.0
Pillow>=10.0.0
trimesh>=4.0.0

# For training (optional)
# torch>=2.0.0
# gsplat>=1.0.0
# plyfile>=1.0.0

# For rendering with Blender (optional)
# Run: blender --background --python render_views.py
# bpy and mathutils are available inside Blender's Python environment
